# BF_WEBSITE_ENV_SINGLE_SOURCE_v1
# BF-Website is a static Vite site. Only VITE_* variables reach the bundle.
# api.ts hardcodes the production origin, so these are overrides, not requirements.
VITE_API_BASE_URL=https://server.boreal.financial
VITE_CLIENT_APP_URL=https://client.boreal.financial
