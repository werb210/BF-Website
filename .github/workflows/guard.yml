name: Guardrails

on:
  pull_request:

jobs:
  guard:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - run: chmod +x scripts/guard-no-node-modules-tests.sh
      - run: ./scripts/guard-no-node-modules-tests.sh
